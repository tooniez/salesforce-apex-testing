@isTest
public class UtilityTestClass {
    @isTest static void testUtility() {
        // Call a method on the utility class
        MyUtilityClass.myMethod();

        // Assert that the utility method behaved correctly
        // This will depend on what your utility method does
    }
}